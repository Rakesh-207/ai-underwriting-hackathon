import { applicant } from '../lib/fixtures.ts';
import { Page, Panel, Provenance, Notice } from '../components/workspace/index.tsx';
export function Applicant() { return <Page eyebrow="Applicant" title="Synthetic applicant" description="Review the baseline fields used by the deterministic scorecard. This is synthetic fixture data, not a real person's financial profile."><Notice>All values below are synthetic fixture values. Protected traits and proxy features are excluded.</Notice><div className="grid gap-4 md:grid-cols-2"><Panel title="Baseline application" description={applicant.applicantId}><dl className="grid grid-cols-2 gap-4 text-sm">{[['Bureau score', applicant.baseline.bureauScore],['Monthly income', `₹${applicant.baseline.monthlyIncome.toLocaleString()}`],['Monthly debt', `₹${applicant.baseline.monthlyDebt.toLocaleString()}`],['Employment', `${applicant.baseline.employmentMonths} months`],['Completeness', `${Math.round(applicant.baseline.applicationCompleteness * 100)}%`]].map(([label,value]) => <div key={label}><dt className="text-muted">{label}</dt><dd className="mt-1 font-semibold text-ink">{value}</dd></div>)}</dl></Panel><Provenance refs={applicant.provenance} /></div></Page>; }
