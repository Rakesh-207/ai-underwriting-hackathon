import { Link } from 'react-router-dom';
import { Button } from '../components/ui/button.tsx';
import { Metric, Page, Panel, Status } from '../components/workspace/index.tsx';
import { applicant, consentReceipts, score } from '../lib/fixtures.ts';
export function Overview() { return <Page eyebrow="Overview" title="Simulation overview" description="A transparent, synthetic underwriting decision-support workspace. Explore consent, evidence, behavior, fairness, and audit context without producing a real lending outcome."><div className="flex flex-wrap items-center justify-between gap-4"><div><Status band="strong">Simulation only</Status><p className="mt-2 text-sm text-muted">Fixture: synthetic-applicant-v1 · session retention</p></div><Link to="/app/consent"><Button>Start a new simulation</Button></Link></div><div className="grid gap-4 sm:grid-cols-3"><Metric label="Applicant" value={applicant.displayName} detail={applicant.applicantId} /><Metric label="Consent state" value={`${consentReceipts.filter((r) => r.status === 'granted').length}/4 granted`} detail="Purpose-bound receipts" /><Metric label="Latest score" value={`${score.dynamicScore} · ${score.riskBand}`} detail="Higher is stronger reliability" /></div><Panel title="Workflow shortcuts" description="Move through the simulation in a deliberate order."><div className="grid gap-3 sm:grid-cols-3">{[['/app/consent','Consent','Set purpose boundaries'],['/app/score','Score','Review evidence'],['/app/audit','Audit','Trace every event']].map(([href,label,detail]) => <Link className="rounded-md border border-border p-4 hover:bg-bg" key={href} to={href}><p className="font-medium text-ink">{label}</p><p className="mt-1 text-sm text-muted">{detail}</p></Link>)}</div></Panel></Page>; }
