import { score } from '../lib/fixtures.ts';
import { Page, Panel, Provenance, Status, Notice } from '../components/workspace/index.tsx';
export function Score() { return <Page eyebrow="Score" title="Reliability score" description="The scorecard is deterministic and server-owned. Review baseline and consent-gated dynamic contributions with their evidence ledger."><Notice>Simulation only: a higher score indicates stronger synthetic reliability within this demo. It is not an official credit score or lending outcome.</Notice><div className="grid gap-4 sm:grid-cols-3"><Panel title="Baseline score"><p className="text-4xl font-semibold">{score.baselineScore}</p><p className="mt-1 text-sm text-muted">Traditional data only</p></Panel><Panel title="Dynamic score"><p className="text-4xl font-semibold text-primary">{score.dynamicScore}</p><Status band={score.riskBand}>{score.riskBand}</Status></Panel><Panel title="Alternative contribution"><p className="text-4xl font-semibold">+{score.alternativeContribution}</p><p className="mt-1 text-sm text-muted">Consent-gated points</p></Panel></div><Panel title="Evidence ledger" description="Every point contribution links back to a source and explanation."><div className="space-y-3">{score.evidence.map((item) => <div key={item.featureKey} className="grid gap-2 border-b border-border pb-3 last:border-0 sm:grid-cols-[1fr_auto]"><div><p className="font-medium">{item.direction === 'supports' ? '↑' : item.direction === 'reduces' ? '↓' : '→'} {item.label} <span className="ml-2 text-xs text-muted">{item.direction}</span></p><p className="text-sm text-muted">{item.explanation}</p></div><p className={item.signedPoints >= 0 ? 'font-semibold text-success' : 'font-semibold text-danger'}>{item.signedPoints > 0 ? '+' : ''}{item.signedPoints} pts</p></div>)}</div></Panel><Provenance refs={score.provenance} /></Page>; }
